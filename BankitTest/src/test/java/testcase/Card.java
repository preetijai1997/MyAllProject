package testcase;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.testng.annotations.Test;

import base.TestBase;

public class Card extends TestBase{
	/*
	@Test(priority=2)
	public void CCPayment()
	{
		driver.findElement(By.xpath(OR.getProperty("CC")));
		
		List<WebElement> CCPayment= driver.findElements(By.xpath(OR.getProperty("Option")));
		
		for(int i=0;i<CCPayment.size();i++)
		{
			if(CCPayment.get(i).getText().equals("Option 1"))
			{
				CCPayment.get(i).click();
			}
		}
		
		
	}
*/
}
